"use client";

import { SignOutButton } from "./SignOutButton";

/** @deprecated Use SignOutButton — kept for AppShell solo flow. */
export function LogoutButton() {
  return <SignOutButton label="Logout" />;
}
